# HPLC-DAD 콜대원 아세트아미노펜 분석 프로젝트 의존성
# 설치: pip install -r requirements.txt

pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
scipy>=1.10.0
openpyxl>=3.1.0        # Excel(.xlsx) 원본 데이터 읽기용

# 3D 인터랙티브(HTML) 뷰어를 함께 쓰는 경우 (선택)
plotly>=5.20.0

# 참고: 회전 mp4 저장(hplc_3d_viewer.py의 SAVE_ROTATION_VIDEO=True)은
# 시스템에 FFmpeg가 별도 설치되어 있어야 합니다 (pip으로 설치되지 않음).
# https://ffmpeg.org/download.html
